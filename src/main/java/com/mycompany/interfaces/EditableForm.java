package com.mycompany.interfaces;

public interface EditableForm {

	//Sólo para los formularios que se editen posteriormente, para editar el objeto se va a precisar llamar al ID
	public Long getId();
	
}
